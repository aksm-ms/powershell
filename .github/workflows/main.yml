name: deploy to static app
on:
  push:
    branches: [ convert-to-securestring-issue ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Azure login
      run: |
         /usr/bin/pwsh -Command ConvertTo-SecureString "aT+i#" -AsPlainText -Force
      
      
