name: CI
on:
  push:
    paths: 
      - '.github/workflows/main.yml'
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Azure PS login
#       uses: aksm-ms/login@login-perf
      uses: azure/login@v1.1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
        enable-AzPSSession: true
    - name: testing
      run: |
         Get-AzContext
      shell:
        pwsh
