name: CI
on:
  push:
    branches: [ psql ]
    
jobs:

  build-and-deploy:
    runs-on: ${{ matrix.os }}
    strategy:
        matrix:
          os: [ubuntu-latest, macos-latest]
    steps:
    - uses: actions/checkout@v2.2.0
    - name: 'running psql'
      run: |
        psql -f ${{github.workspace}}/run.sql "${{secrets.AZURE_POSTGRES_CONN_STRING}}"
        #psql -f ${{github.workspace}}/run.sql -L ak.sql "${{secrets.AZURE_POSTGRES_CONN_STRING}}"
        
      
      
