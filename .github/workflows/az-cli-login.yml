name: CI
on:
  push:
    paths:
      - '.github/workflows/az-cli-login.yml'
    
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: az login v1
      uses: Azure/login@users/aksm/az-version
#       uses: aksm-ms/login@master
#       uses: Azure/login@v1
      with:
#         creds: ${{ secrets.AZURE_CREDENTIALS_MGMT }}
        creds: ${{secrets.AZURE_CREDENTIALS}}
#         enable-AzPSSession: true
    
    - run: |
        az account show
        
#     - name: Run Azure PowerShell script
#       uses: azure/powershell@v1
#       with:
#         azPSVersion: '3.1.0'
#         inlineScript: |
#           Get-AzVM -ResourceGroupName "akshaya-tests"
