name: CI
on:
  push:
    branches: [ login-time ]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Azure PS login
      uses: azure/login@v1.1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
        enable-AzPSSession: true
