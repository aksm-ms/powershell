name: CI
on:
  push:
    branches: [ psql ]
    
jobs:

  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.2.0
    - run: |
        psql -f ${{github.workspace}}/run.sql "host=ak-db.postgres.database.azure.com port=5432 dbname=postgres user=aksm@ak-db password=Mads.handyvs!2 sslmode=require"
